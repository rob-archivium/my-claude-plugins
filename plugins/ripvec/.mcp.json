{
  "mcpServers": {
    "ripvec": {
      "command": "bash",
      "args": [
        "-lc",
        "set -euo pipefail; root=\"${CLAUDE_PLUGIN_ROOT:-$PWD}\"; cd \"$root\"; exec ./bin/ensure-ripvec-mcp.sh \"$@\"",
        "ripvec-mcp"
      ],
      "cwd": "."
    }
  }
}
