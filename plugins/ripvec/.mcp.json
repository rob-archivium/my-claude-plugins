{
  "mcpServers": {
    "ripvec": {
      "command": "bash",
      "args": [
        "-lc",
        "set -eo pipefail; root=\"${CLAUDE_PLUGIN_ROOT}\"; case \"$root\" in ''|*'$'*) root=\"$PWD\" ;; esac; cd \"$root\"; exec ./bin/ensure-ripvec-mcp.sh \"$@\"",
        "ripvec-mcp"
      ],
      "cwd": "."
    }
  }
}
