{
  "mcpServers": {
    "ripvec": {
      "command": "sh",
      "args": ["-c", "BIN=\"${CLAUDE_PLUGIN_ROOT}/bin/ripvec-mcp\"; test -x \"$BIN\" || bash \"${CLAUDE_PLUGIN_ROOT}/bin/ensure-ripvec-mcp.sh\" --install-only >&2; exec \"$BIN\""]
    }
  }
}
