{
  "mcpServers": {
    "ripvec": {
      "command": "bash",
      "args": [
        "-lc",
        "set -euo pipefail; root=\"${PLUGIN_ROOT_HINT:-}\"; case \"$root\" in ''|*'$'*) root=\"$PWD\" ;; esac; cd \"$root\"; exec ./bin/ensure-ripvec-mcp.sh \"$@\"",
        "ripvec-mcp"
      ],
      "cwd": ".",
      "env": {
        "PLUGIN_ROOT_HINT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
